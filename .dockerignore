node_modules
.git
.env
.env.prod
.claude

# Rebuilt inside the image by the `build` stage; copying the host's output in
# would let a stale local build win over a fresh one.
dist

# User uploads live in a volume, not in the image.
public/uploads

# Served from a bind mount, not from the image — see docker-compose.yml.
# Excluding it drops ~29MB from the build context.
random-imgs

# Raw source material and scratch files — none of it is served.
pngs-inti-blogs
pngs-inti-proyectos
pngs blog.zip
pngs proyectos.zip
__MACOSX
misc
*.md
notas-*.txt
cardinal-notas-finales.txt
database-scheme.txt
